"use client";import {useState} from 'react';import {createClient} from '@/lib/supabase/client';export default function Signup(){const [s,setS]=useState('');async function go(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await createClient().auth.signUp({email:String(f.get('email')),password:String(f.get('password')),options:{data:{full_name:String(f.get('name')),role:String(f.get('role'))}}});setS(error?error.message:'Check your email to confirm your account.')}return <main className="container"><form className="card form" onSubmit={go}><h1>Create account</h1><input className="input" name="name" placeholder="Full name" required/><input className="input" name="email" type="email" placeholder="Email" required/><input className="input" name="password" type="password" minLength={8} placeholder="Password" required/><select className="input" name="role"><option value="athlete">Athlete</option><option value="coach">Coach</option></select><button className="btn">Create account</button><p>{s}</p></form></main>}
