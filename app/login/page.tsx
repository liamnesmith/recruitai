"use client";import {useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';export default function Login(){const [e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState('');const r=useRouter();async function go(x:any){x.preventDefault();setM('Signing in…');const {error}=await createClient().auth.signInWithPassword({email:e,password:p});if(error)setM(error.message);else{r.push('/dashboard');r.refresh()}}return <main className="container"><form className="card form" onSubmit={go}><h1>Log in</h1><input className="input" type="email" placeholder="Email" onChange={x=>setE(x.target.value)} required/><input className="input" type="password" placeholder="Password" onChange={x=>setP(x.target.value)} required/><button className="btn">Log in</button><p>{m}</p></form></main>}
