import Link from 'next/link';import {requireUser} from '@/lib/auth';import {scoreAthlete} from '@/lib/scoring';export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const {supabase}=await requireUser();let query=supabase.from('athlete_directory').select('*').limit(50);if(q)query=query.or(`full_name.ilike.%${q}%,school.ilike.%${q}%,position.ilike.%${q}%`);const {data=[]}=await query;return <><h1>Athlete search</h1><form><input className="input" name="q" defaultValue={q} placeholder="Name, school or position"/><button className="btn">Search</button></form><div className="grid">{data?.map((a:any)=>{const s=scoreAthlete(a);return <Link className="card" href={`/athletes/${a.id}`} key={a.id}><span className="score">{s.overall}</span><h3>{a.full_name}</h3><p>{a.sport} • {a.position}</p><p>{a.school} • {a.state} • {a.grad_year}</p><small>Confidence {s.confidence}/100</small></Link>})}</div></>}
